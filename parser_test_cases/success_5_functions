{
    if (true) {
        bvec3 bv3 = 43;
        bvec4 bv4;
        /* Functions should be able to accept any number of statements */
        int i = dp3();
    } else {
        int j =lit(2,e,5,6,g,7);
        float f =  rsq(4-3*2);
        bool b= dp3();
    }
}
